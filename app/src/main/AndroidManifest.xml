<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <!-- required permissions to allow tv and touchscreen devices if available -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    
    <!-- required permission to allow devices without wifi -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    
    <!-- required permissions to control ADB -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" />
    
    <!-- required permission to prevent losing permissions -->
    <uses-permission android:name="android.permission.WHITELIST_AUTO_REVOKE_PERMISSIONS" tools:ignore="ProtectedPermissions" />

    <!-- required permission to receive the boot completed broadcast -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- required permissions to run the foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- required permissions to run the foreground service from boot completed -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- required permission to run the foreground service from the background -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- required to read the LAN IP address -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- required to open socket connections -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- required for ADB mDNS detection -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" tools:ignore="LeanbackUsesWifi" />

    <!-- Main Activity Declaration -->
    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AutoADB"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:extractNativeLibs="true"
        tools:targetApi="33"
        tools:ignore="DataExtractionRules">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Broadcast Receiver Declaration - This receiver listens for the BOOT_COMPLETED action. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.REBOOT"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="au.com.inoahguy.autoadb.TEST_BOOT_RECEIVER" /> <!-- ** FOR TESTING ONLY ** -->
            </intent-filter>
        </receiver>

        <!-- Foreground Service Declaration -->
        <service android:name=".BootForegroundService" android:exported="false" android:foregroundServiceType="specialUse" />

        <!-- Pairing Accessibility Service Declaration -->
        <service
            android:name=".AdbPairingAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label"
            android:exported="true">
        <intent-filter>
            <action android:name="android.accessibilityservice.AccessibilityService" />
        </intent-filter>
        <meta-data
            android:name="android.accessibilityservice"
            android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>